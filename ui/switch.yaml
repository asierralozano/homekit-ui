identifier: homekit-ui
name: HKSwitch
template: switch

properties:
  entity:
    limited_type: str
    optional: false
  entity_name:
    limited_type: str
  entity_icon:
    limited_type: str
    default: mdi:power-plug

outputs:
  out:
    limited_type: card